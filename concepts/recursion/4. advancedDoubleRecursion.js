/**
 * This is part of a series see ./ 0.recursion file for introduction on the series
 *
 * Things explained in this file
 *     a. calling double recursive functions with or    recurse(n) || recurse(n - 2),
 *     b. calling double recursive functions with and   recurse(n) && recurse(n - 2)
 */

/** a. -------------------calling double recursive functions with or---------------------------------------------------*/
/** b. -------------------calling double recursive functions with and---------------------------------------------------*/
