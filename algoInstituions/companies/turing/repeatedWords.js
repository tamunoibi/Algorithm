function weight(target, arr) {
  const numberOfOccurence = (i, arr) => {
    let count = 0;
    // I want a copy from i to the end
    // eg. arr = aaapp; i = 2
    // newArr = app
    const newArr = arr.slice(i, arr.length);
    newArr.forEach((item) => {

    })
    return count;
  };
  let maxCount = 0;
  if ((arr.slice(i), targe.length === target)) {
    maxCount = Math.max(numberOfOccurence(i, arr), arr);
  }
}
